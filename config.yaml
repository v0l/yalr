server:
  host: "0.0.0.0"
  port: 3000

database:
  url: "sqlite:/app/data/llm_router.db?mode=rwc"

# NIP-98 authentication (enabled for testing)
auth:
  enabled: true
  # allowed_pubkeys:
  #   - "your_pubkey_here"
